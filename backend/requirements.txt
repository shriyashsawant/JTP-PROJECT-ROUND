fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
sentence-transformers==3.4.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
numpy==2.2.1
httpx==0.28.1
prometheus-client==0.25.0
structlog==25.1.0
Pillow==11.1.0

# Schema migrations (backend/app/db/migrations/). Alembic needs its own
# synchronous driver (psycopg2) for running DDL - independent of asyncpg,
# which remains the only DB driver on the actual request-serving path.
alembic==1.18.5
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
